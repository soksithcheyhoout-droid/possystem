services:
  - type: web
    name: possystem
    runtime: php
    plan: free
    buildCommand: composer install --no-dev && php artisan migrate --force
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: DB_CONNECTION
        value: sqlite
